Note: verificare che tutto ciò che è presente in questo file sia rispettato prima della consegna del
        progetto di Tecnologie Web 15/02/2019.


-> Vedi Specifiche progetto se si sono rispettati tutti i punti:
https://elearning.unipd.it/math/pluginfile.php/43657/mod_resource/content/1/Specifiche%20Progetto.pdf

->se viene usato input file, il tag form di apertura deve contenere l'attributo enctype="multipart/form-data"
    (ovvero invio di dati al server non solo testuali)
->bottone per resettare/ripulire i form dai dati
->attributi readonly/disabled campi dei form non editabili da inserire
->controllare di avere inserito la lingua che il sito utilizza
->usare "img" solo su immagini che riguardano il contenuto e "background" quelle che riguardano il solo layout
->controllare che ci sia alt per tutte le immagini e anche il logo(il logo deve riportare alla home);
    (alt vuoto per immagini che riguardano il solo layout)
->aggiungere simbolo W3C tripla A se conforme alla validazione in tripla A
->provare a vedere senza immagini se le pagine rimangono accessibili
->provare a vedere senza JS se le pagine reggono
->provare a vedere senza CSS se le pagine reggono
->provare a stampare la pagina e far evidenziare a uno conosciuto i link
->eliminare commenti da HTML e CSS riguardanti cose non utilizzate
->accessKey tabindex che indicano un carattere su cui posizionare il focus del link e l'ordine di 
    tabulazione(accessibilità per chi non può usare il mouse)
->inserire longdesc sulle immagini??
->tutti i textarea devono avere OBBLIGATORIAMENTE cols e rows
->verificare che il documento sia strutturato bene vedeendo il sommario generato automaticamente
    http://gsnedders.html5.org/outliner/
->autocomplete/autofocus sui form?
->per le immagini racchiudere il tag img dentro al tag <figure> accompagnato da 
    <figcaption>(non ci sarà quindi la necessità di mettere l'alt se si usa questo tag)
->file .manifest???(cache manifest) elenca le risorse disponibili anche in assenza di  connessione alla rete

->casella di ricerca
->uniformare i bottoni: che tutti i bottoni abbiano lo stesso layout
->topic senza argomenti su breadcrumb

!!! vedere ultime slide PHP per regole consegna e relazione

->colori link(da motivare nella relazione)
->accessKey(vedi issue)
->lingua inglese
->check javascript
->relazione(compatibilità tag html5/versioni browser Giacomo G.) + leggere
->dividere css per ogni media query/dispositivo